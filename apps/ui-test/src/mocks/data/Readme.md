# Data

The json files in this folder are all created by running `pnpm createData`. Don't bother editing the data files by hand.

The only re-run createData to change the standard data used in the example.
